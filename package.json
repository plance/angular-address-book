{
	"devDependencies": {
		"http-server": "*",
		"bower": "*",
		"gulp": "*",
		"gulp-sass": "*"
	},
	"scripts": {
		"postinstall": "bower install",

		"prestart": "npm install",
		"start": "http-server -a localhost site -p 8000 -c-1"
	}
}
